name: Project-0
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Build with Maven
      run: mvn -B package --file project-0/pom.xml
    - run: mkdir artifacts && cp target/*.wa​​r artifacts
    - uses: actions/upload-artifact@v2
      with:
        name: Package
        path: artifacts
